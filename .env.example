# copy this file to .env and fill in your own values

# --- database connection ---------------------------------------------------
# Primary connection string for MongoDB Atlas or any MongoDB server.
# can also use the generic names DATABASE_URL or MONGO_URL.
# if you prefer MySQL-compatible backend you may supply a mysql:// URL,
# but MongoDB is recommended for the free‑tier Cloud offerings.#
# if you already have a file named `.txt` in the project root containing
# connection details (e.g. cluster URI on the last line), the app will read
# that automatically when no env var is set.MONGODB_URI=

# --- session and security --------------------------------------------------
SESSION_SECRET=some_long_random_string  # generate a long random value

# optional limit on tracking entries returned by /api/tracking (default 5000)
#TRACKING_MAX_EVENTS=5000

# --- admin / auth ----------------------------------------------------------
# list of usernames separated by commas
AUTH_USERS=admin
# password for each user
AUTH_USER_ADMIN_PASSWORD=changeme
# you can add more users with additional AUTH_USER_<NAME>_PASSWORD vars

# --- WebAuthn / passkeys ---------------------------------------------------
WEBAUTHN_RP_NAME=Macerata FotoMap
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:3000

# --- optional application settings ----------------------------------------
# ADMIN_USERNAME, ADMIN_PASSWORD are used for the built‑in admin user
# (default admin/change_this_password)
#PORT=3000         # only needed when running locally

# --- comments --------------------------------------------------------------
# after editing this file, restart the server to pick up new values.
